"use client";
import React from "react";
const Add = () => {
  return (
    <div>
      <button className="btn btn-primary" onClick={() => {}}>
        Add
      </button>
    </div>
  );
};

export default Add;
